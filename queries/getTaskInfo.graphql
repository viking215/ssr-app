query GET_TASK_INFO ($id: Int!) {
    tasks_by_pk(id: $id) {
        created_at
        description
        id
        isDone
        task_user {
            avatar_url
        }
        title
        updated_at
    }
}